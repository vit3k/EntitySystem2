{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "options": {
                "cwd": "EntitySystem2"
            },
            "command": "make",
            "problemMatcher": {
                "base": "$gcc",
                "fileLocation": ["relative", "${workspaceRoot}/EntitySystem2"]
            },
            "group": {
                "kind": "build",
                "isDefault": true,
            }
        }
    ]
}